FROM node:22-alpine

WORKDIR /app

# package.json is needed so Node treats .js files as ES modules (type: module).
COPY package.json ./
COPY server ./server
COPY data ./data

ENV NODE_ENV=production
ENV API_PORT=8787

EXPOSE 8787

CMD ["node", "server/index.js"]
